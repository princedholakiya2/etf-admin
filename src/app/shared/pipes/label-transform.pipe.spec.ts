import { LabelTransformPipe } from './label-transform.pipe';

describe('LabelTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
